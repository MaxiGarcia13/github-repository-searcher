interface RepositoriesListProps {
  loading?: boolean;
}

const RepositoriesList = (props: RepositoriesListProps): React.ReactElement => {
  return (
    <ul>
      <li>Hola</li>
    </ul>
  );
};

export default RepositoriesList;
